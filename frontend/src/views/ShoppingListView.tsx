import { Heading, VStack } from "@chakra-ui/react";

const ShoppingListView = () => {
  return (
    <VStack align="start" spacing={4}>
      <Heading size="lg">Lista zakupów</Heading>
    </VStack>
  );
};

export default ShoppingListView;
